import React from "react";
import {
  Routes,
  Route,
  Link,
} from "react-router-dom";

const WebRemoteContainer = ({ }) => {
  
    return (
      <div>WebRemoteContainer in NextJS remote example</div>
    );

  };

  export default WebRemoteContainer;